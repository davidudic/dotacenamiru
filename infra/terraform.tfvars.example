# Copy to terraform.tfvars (gitignored) and fill in. The API token is NOT here —
# provide it via the environment:  export TF_VAR_cloudflare_api_token=...

cloudflare_account_id = "your-cloudflare-account-id"
# name_prefix = "marketing-microsite"
